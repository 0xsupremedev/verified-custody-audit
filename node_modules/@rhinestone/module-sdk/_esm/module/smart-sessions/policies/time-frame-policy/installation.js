import { encodePacked } from 'viem';
import { GLOBAL_CONSTANTS } from '../../../../constants.js';
export const getTimeFramePolicy = (param) => {
    return {
        policy: GLOBAL_CONSTANTS.TIME_FRAME_POLICY_ADDRESS,
        address: GLOBAL_CONSTANTS.TIME_FRAME_POLICY_ADDRESS,
        initData: encodePacked(['uint48', 'uint48'], [param.validUntil, param.validAfter]),
    };
};
//# sourceMappingURL=installation.js.map